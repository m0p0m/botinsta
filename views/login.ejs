<<<<<<< HEAD
<div class="login-container">
  <div class="login-form">
    <div class="login-header">
      <h1>🤖 ربات اینستاگرام</h1>
      <p class="subtitle">ورود امن به حساب</p>
    </div>

    <% if(typeof error !== 'undefined' && error) { %>
      <div class="alert alert-error">
        <span class="alert-icon">⚠️</span>
        <div class="alert-content">
          <%= error %>
        </div>
      </div>
    <% } %>

    <form action="/add-account" method="POST" class="login-form-group">
      <div class="form-group">
        <label for="username">نام کاربری Instagram</label>
        <input 
          type="text" 
          id="username"
          name="username" 
          placeholder="نام کاربری خود را وارد کنید" 
          autocomplete="username" 
          required
          minlength="3"
          maxlength="30"
        >
        <small class="form-hint">نام کاربری بدون @ وارد کنید</small>
      </div>

      <div class="form-group">
        <label for="password">رمز عبور</label>
        <input 
          type="password" 
          id="password"
          name="password" 
          placeholder="رمز عبور خود را وارد کنید" 
          autocomplete="current-password" 
          required
          minlength="6"
        >
        <small class="form-hint">رمز عبور شما کاملاً ایمن است</small>
      </div>

      <button type="submit" class="btn btn-primary btn-large">
        <span class="btn-icon">🔐</span>
        <span>ورود</span>
      </button>
    </form>

    <div class="login-info">
      <div class="info-item">
        <span class="info-icon">ℹ️</span>
        <span>اگر دو مرحله‌ای فعال دارید، اپ Instagram را بررسی کنید</span>
      </div>
      <div class="info-item">
        <span class="info-icon">✅</span>
        <span>رمز عبور شما تنها برای لاگین استفاده می‌شود</span>
      </div>
      <div class="info-item">
        <span class="info-icon">🔒</span>
        <span>Session شما محفوظ ذخیره می‌شود</span>
      </div>
    </div>
  </div>
=======
<div class="login-form">
  <h1>ورود به ربات اینستاگرام</h1>
  <form action="/login" method="POST">
    <input type="text" name="username" placeholder="نام کاربری" autocomplete="username" required>
    <input type="password" name="password" placeholder="رمز عبور" autocomplete="current-password" required>
    <button type="submit">ورود</button>
  </form>
  <% if(typeof error !== 'undefined' && error) { %>
    <p class="form-error"><%= error %></p>
  <% } %>
>>>>>>> e21c7d3b58513bd702873f009d433ccf3ba328f0
</div>

<style>
.login-container {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: calc(100vh - 100px);
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  padding: 20px;
}

.login-form {
  background: white;
  border-radius: 12px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
  padding: 40px;
  width: 100%;
  max-width: 400px;
}

.login-header {
  text-align: center;
  margin-bottom: 30px;
}

.login-header h1 {
  font-size: 28px;
  color: #333;
  margin: 0 0 8px 0;
  font-weight: 600;
}

.login-header .subtitle {
  color: #666;
  font-size: 14px;
  margin: 0;
}

.alert {
  padding: 12px 15px;
  border-radius: 8px;
  margin-bottom: 20px;
  display: flex;
  align-items: flex-start;
  gap: 10px;
}

.alert-error {
  background-color: #fee;
  border-left: 4px solid #f44336;
  color: #c33;
}

.alert-icon {
  font-size: 18px;
  flex-shrink: 0;
}

.alert-content {
  font-size: 14px;
  line-height: 1.5;
  white-space: pre-line;
}

.form-group {
  margin-bottom: 20px;
}

.form-group label {
  display: block;
  font-weight: 600;
  color: #333;
  margin-bottom: 6px;
  font-size: 14px;
}

.form-group input {
  width: 100%;
  padding: 12px;
  border: 2px solid #ddd;
  border-radius: 6px;
  font-size: 14px;
  transition: all 0.3s ease;
  box-sizing: border-box;
}

.form-group input:focus {
  outline: none;
  border-color: #667eea;
  box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
}

.form-hint {
  display: block;
  color: #999;
  font-size: 12px;
  margin-top: 4px;
}

.btn {
  width: 100%;
  padding: 12px;
  border: none;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
}

.btn-primary {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
}

.btn-primary:active {
  transform: translateY(0);
}

.btn-large {
  padding: 14px;
}

.btn-icon {
  font-size: 16px;
}

.login-info {
  margin-top: 30px;
  padding-top: 20px;
  border-top: 1px solid #eee;
}

.info-item {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 13px;
  color: #666;
  margin-bottom: 10px;
}

.info-item:last-child {
  margin-bottom: 0;
}

.info-icon {
  font-size: 16px;
  flex-shrink: 0;
}

@media (max-width: 480px) {
  .login-form {
    padding: 25px;
  }

  .login-header h1 {
    font-size: 24px;
  }
}
</style>
