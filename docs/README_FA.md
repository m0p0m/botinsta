# 🤖 ربات اینستاگرام - Instagram Automation Bot

ربات خودکار اینستاگرام با داشبورد حرفه‌ای برای مدیریت چندین حساب، مشاهده داده‌های پروفایل و خودکارسازی تعاملات مبتنی بر هشتگ.

## ✨ ویژگی‌ها

- ✅ **مدیریت چند حساب:** اضافه کردن و تبدیل بین چند حساب Instagram
- ✅ **داده‌های پروفایل:** مشاهده آمار پروفایل (دنبال‌کنندگان، دنبال‌شوندگان، تعداد پست)
- ✅ **خودکارسازی مبتنی بر هشتگ:** لایک اتوماتیک بر روی کامنت‌های مرتبط با هشتگ
- ✅ **تأخیرهای تصادفی:** جلوگیری از محدودیت‌های Instagram با تأخیرهای هوشمند
- ✅ **به‌روزرسانی زنده:** نمایش وضعیت فعالیت ربات در زمان واقعی
- ✅ **رابط کاربری مشابه اپ:** طراحی مشابه با اپ اصلی Instagram
- ✅ **Error Handling حرفه‌ای:** پیام‌های خطا به فارسی و پیشنهادات حل
- ✅ **Security:** رمز عبور محفوظ و تنها برای لاگین استفاده می‌شود

---

## 🚀 شروع سریع

### نصب

```bash
# کلون کردن پروژه
git clone https://github.com/m0p0m/botinsta.git
cd botinsta

# نصب وابستگی‌ها
npm install
```

### اجرا

```bash
# شروع اپلیکیشن
npm start

# یا در حالت توسعه
npm run dev
```

سرور در `http://localhost:3000` اجرا می‌شود

---

## 📱 نحوه استفاده

### 1. ورود به حساب Instagram

1. برنامه را باز کنید
2. نام کاربری و رمز عبور Instagram را وارد کنید
3. اگر دو مرحله‌ای فعال دارید، اپ Instagram را چک کنید

### 2. مشاهده داشبورد

- پروفایل، دنبال‌کنندگان و آمار دیگر را ببینید
- بین حساب‌های متعدد تبدیل کنید

### 3. مدیریت هشتگ‌ها

- هشتگ‌های جدید اضافه کنید
- هشتگ‌های قدیمی را حذف کنید

### 4. خودکارسازی

- هشتگ و حساب را انتخاب کنید
- دکمه "شروع" را کلیک کنید
- وضعیت به‌صورت زنده نمایش داده می‌شود

---

## 🔐 خطاهای معمول

| خطا | دلیل | حل |
|-----|------|-----|
| 🔐 تأیید دو مرحله‌ای | حساب محافظت شده | اپ Instagram را چک کنید |
| ❌ نام کاربری اشتباه | اطلاعات ورود غلط | اطلاعات خود را دوباره بررسی کنید |
| ⏳ بسیار زیاد تلاش | محدودیت Instagram | 15-30 دقیقه صبر کنید |
| 🌐 خطای اتصال | مشکل اینترنت | اتصال را بررسی کنید |

برای اطلاعات کامل: 👉 [LOGIN_GUIDE.md](./LOGIN_GUIDE.md)

---

## 🧪 تست

```bash
# تست سرویس لاگین
npm run test:login

# تست جامع
npm test
```

---

## 📁 ساختار پروژه

```
botinsta/
├── index.js                 # نقطه ورود برنامه
├── config.js               # تنظیمات تمامی موارد
├── routes/
│   └── index.js            # تمام روت‌های API
├── services/
│   ├── instagram.service.js     # سرویس لاگین و API
│   ├── error-handler.service.js # مدیریت خطاها
│   ├── bot.service.js           # سرویس ربات
│   ├── hashtag.service.js       # مدیریت هشتگ‌ها
│   └── logger.service.js        # سیستم لاگ کردن
├── views/
│   ├── login.ejs           # صفحه ورود
│   ├── accounts.ejs        # مدیریت حساب‌ها
│   ├── hashtags.ejs        # مدیریت هشتگ‌ها
│   ├── bot.ejs             # کنترل ربات
│   ├── dashboard.ejs       # داشبورد اصلی
│   └── layout.ejs          # الگو پایه
├── public/
│   └── css/
│       └── style.css       # استایل‌های UI
├── data/
│   ├── accounts.json       # ذخیره حساب‌های لاگین شده
│   └── hashtags.json       # لیست هشتگ‌ها
├── tests/
│   ├── instagram.service.test.js
│   └── login.service.test.js
└── logs/                   # فایل‌های لاگ برنامه
```

---

## 🔧 تنظیمات

فایل `config.js` تمام تنظیمات را کنترل می‌کند:

```javascript
{
  TIMEOUT: {
    LOGIN: 30000,              // timeout لاگین (30 ثانیه)
    API_REQUEST: 15000,        // timeout درخواست API (15 ثانیه)
    DELAY_MIN: 800,            // حداقل تأخیر (ms)
    DELAY_MAX: 1200,           // حداکثر تأخیر (ms)
  },
  INSTAGRAM: {
    PRE_LOGIN_DELAY: 1000,     // تأخیر Pre-Login
    POST_LOGIN_DELAY: 1500,    // تأخیر Post-Login
  },
  VALIDATION: {
    USERNAME_MIN_LENGTH: 3,
    PASSWORD_MIN_LENGTH: 6,
  }
}
```

---

## 🔒 ایمنی

### رمز عبور
- ❌ رمز عبور ذخیره نمی‌شود
- ✅ تنها برای لاگین اولیه استفاده می‌شود
- ✅ Session محفوظ ذخیره می‌شود

### ارتباطات
- ✅ تمام ارتباطات رمزشده (HTTPS)
- ✅ Instagram Private API معتبر
- ✅ Device fingerprint واقعی

---

## 📊 لاگ‌ها

برنامه تمام درخواست‌ها و خطاها را ثبت می‌کند:

```
logs/
├── errors-YYYY-MM-DD.json
└── successful-logins.json
```

---

## 🛠️ وابستگی‌ها

```json
{
  "express": "^5.1.0",
  "instagram-private-api": "^1.46.1",
  "express-session": "^1.18.2",
  "body-parser": "^2.2.0",
  "ejs": "^3.1.10",
  "ws": "^8.18.3"
}
```

---

## 📝 نکات مهم

### ⚠️ قانونی
- تنها برای استفاده شخصی
- نقض شرایط خدمات Instagram نیست
- استفاده در انبوه ممنوع

### 🛡️ بهترین روش‌ها
- از تأخیرهای تصادفی استفاده کنید
- سرعت عمل را محدود کنید
- حساب‌های مختلف را بچرخانید

---

## 📞 کمک و پشتیبانی

مشکلی پیدا کردید؟

1. [LOGIN_GUIDE.md](./LOGIN_GUIDE.md) را بخوانید
2. `npm run test:login` را اجرا کنید
3. خطاها را در `logs/` بررسی کنید

---

## 📄 لایسنس

ISC

---

## 👨‍💻 نویسنده

[m0p0m](https://github.com/m0p0m)

---

**آخرین بروزرسانی:** تاریخ کنونی
